import styled from 'styled-components';

const Styled = {
  Game: styled.div`
    align-items: center;
    display: flex;
    height: 100vh;
    justify-content: center;
    width: 100vw;
  `
}

export default Styled;